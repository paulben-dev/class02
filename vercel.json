{
  "installCommand": "npm install && cd teacher && npm install",
  "buildCommand": "cd teacher && npm run build",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/teacher/(.*)", "destination": "/teacher/dist/$1" }
  ]
}
